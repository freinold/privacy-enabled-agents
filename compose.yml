name: privacy-enabled-agents

services:
  valkey:
    image: valkey/valkey:8-alpine@sha256:d827e7f7552cdee40cc7482dbae9da020f42bc47669af6f71182a4ef76a22773
    ports:
      - "6379:6379"
    volumes:
      - ./valkey-data:/data:rw
    command: [ "--appendonly", "yes" ] # Enable data persistence
    restart: on-failure
    healthcheck:
      test: [ "CMD", "valkey-cli", "ping" ]
      interval: 5s
      timeout: 5s
      retries: 5
    environment:
      - VALKEY_SAVE=60 1 # Save DB every 60 seconds if at least 1 change
    labels:
      - "com.privacy-enabled-agents.description=Valkey storage backend"

  redis:
    image: redis:8-alpine@sha256:987c376c727652f99625c7d205a1cba3cb2c53b92b0b62aade2bd48ee1593232
    ports:
      - "6380:6379"
    restart: on-failure
    labels:
      - "com.privacy-enabled-agents.description=Redis checkpointer backend"

  gliner-api:
    image: ghcr.io/freinold/gliner-api:latest@sha256:dc00a437c096adfe8e679a5f5c658febb9f673f063f63957542b1c27a8e1d5c9
    ports:
      - "8081:8080" # API and Gradio frontend
      - "9091:9090" # Metrics endpoint
    stdin_open: true
    tty: true
    volumes:
      - "${HOME}/.cache/huggingface:/app/huggingface:rw"
      - "./gliner-api-configs/quantized_no-entities.yaml:/app/config.yaml:ro"
    environment: []

  privacy-enabled-agents:
    image: ghcr.io/freinold/privacy-enabled-agents:latest@sha256:045c119ae16b2bf3d389a1cf8db5b572a32b29670adbc397989415b27240f31c
    ports:
      - "8080:8080"
    stdin_open: true
    tty: true
    env_file:
      - .env

volumes:
  valkey-data:
    driver: local
